<Window x:Class="DiplomZabpo43.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiplomZabpo43" 
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
 Title="TopMed ©Все права защищены" WindowStartupLocation="CenterScreen" Topmost="True" ResizeMode="CanResizeWithGrip"
      Height="700" Width="800" Icon="/unnamed.jpg" >

    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="700" />

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition  Width="710"/>
        </Grid.ColumnDefinitions>




        <Image x:Name="LogoImage" HorizontalAlignment="Center" Source="/Logo.PNG" Height="66" VerticalAlignment="Top" Width="98" Margin="0,5,0,0"/>

        <StackPanel Orientation="Vertical" x:Name="LeftButtons" Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" Margin="0,0,0,0"
            VerticalAlignment="Center" HorizontalAlignment="Stretch">

            <!--Список кнопок-->
            <!-- Кнопка "Скидки" -->

          

            <!-- Кнопка "Каталог" -->
            <Button x:Name="CatalogButton" HorizontalAlignment="Center" Margin="0,150,0,0" Height="60" Width="70" Style="{StaticResource MenuButton}" Click="CatalogButton_Click">
                <StackPanel Orientation="Vertical">
                    <fa:ImageAwesome Icon="Reorder" Spin="False" SpinDuration="10" Foreground="Black" Width="30" Height="30"/>
                    <TextBlock x:Name="Catalog" Text="Каталог" FontSize="20" Margin="0,0,0,0" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Opacity" Value="0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=CatalogButton}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Button>
            <!-- Кнопка "Поиск" -->
            <Button x:Name="SearchButton" HorizontalAlignment="Center" Margin="0,50,0,0" Height="60" Width="70" Style="{StaticResource MenuButton}" Click="SearchButton_Click">
                <StackPanel Orientation="Vertical">
                    <fa:ImageAwesome Icon="Search" Spin="False" SpinDuration="10" Foreground="Black"  Width="30" Height="30"/>
                    <TextBlock x:Name="Search" Text="Поиск" FontSize="20" Margin="0,0,0,0" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Opacity" Value="0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=SearchButton}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Button>

            <!-- Кнопка "Корзина" -->
            <Button x:Name="CartButton" HorizontalAlignment="Center" Margin="0,50,0,0"  Height="60" Width="70" Style="{StaticResource MenuButton}"  Click="BuscketButton_Click">
                <StackPanel Orientation="Vertical">
                    <fa:ImageAwesome Icon="CartPlus" Spin="False" SpinDuration="10" Foreground="Black"  Width="30" Height="30"/>
                    <TextBlock x:Name="Cart" Text="Корзина" FontSize="17" Margin="0,0,0,0" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Opacity" Value="0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=CartButton}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.5"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Button>

            <!-- Кнопка "Пользователь" -->
            <Button x:Name="UserButton" HorizontalAlignment="Center" Margin="0,90,0,0" Height="60" Width="80" Style="{StaticResource MenuButton}" Click="OpenLoginPage_Click">
                <StackPanel Orientation="Vertical">
                    <fa:ImageAwesome Icon="Lock" Spin="False" SpinDuration="10" Foreground="Black" Width="30" Height="30"/>
                    <TextBlock x:Name="User" Text="Кабинет" FontSize="20" Margin="0,0,0,0" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Opacity" Value="0"/>
                                <Style.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.5"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                </StackPanel>

            </Button>

        </StackPanel>


        <Menu x:Name="UnLogUserMenu" Grid.Row="1" Margin="0,490,710,209" Grid.Column="1" >
            <MenuItem Header=""  x:Name="MenuItemUserUNLOG">
                <MenuItem Header="Войти" Click="LoginAut_Clock"/>
                <MenuItem Header="Зарегистрироваться" Click="Registr_Clock" />
            </MenuItem>
            <Menu.Style>
                <Style TargetType="Menu">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Menu.Style>
        </Menu>
        <Menu x:Name="LogUserMenu" Margin="0,490,710,209" Grid.Column="1" DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}">
            <MenuItem x:Name="MenuItemUserLOG">
                <MenuItem Header="Мой кабинет" Click="OpenProfilenPage_Click"/>
                <MenuItem Header="Выйти" Click="Logout_Click"/>
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
        </Menu>
        <!-- Фрейм для основного содержимого -->
        <Frame  Name="MainFrame" NavigationUIVisibility="Hidden" Grid.Row="0" Margin="0,0,0,0" Grid.Column="1"/>



    </Grid>
</Window>


